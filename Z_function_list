当前和行业标准的核心差距
你现在已经有不错的基础，但和多数正式 WMS/3PL 系统相比，P0 还差这 6 块：

角色权限
Inbound 单据化
库存调整单
Transfer 转移
Cycle Count 盘点
Outbound 取消/冲销，不再真删除
结合你当前代码库，最明显的现状是：

users 还没有 role
stock_movements 的 movement_type 只有 IN / OUT / ADJUST
Outbound 已经开始有 document 结构
Inbound 还主要是 movement 驱动
没有 transfer、count、audit_log 相关表
删除出库现在仍然带有“真删除”思路
P0-1 角色与权限
目标：控制谁能看、谁能改、谁能删、谁能做库存操作。

要改的数据：

users 表加 role
可选再加 is_active
建议角色：

admin
operator
viewer
后端要做：

登录后把 role 返回给前端
给 API 加权限中间件
至少控制这些动作：
删除单据
库存调整
转移
盘点确认
客户/仓库/SKU 主档修改
前端要做：

顶部菜单按角色裁剪
按钮按角色隐藏或禁用
非法操作时显示明确提示
当前代码改动点：

database/schema.sql
backend/internal/service/auth.go
backend/internal/api/server.go
frontend/src/lib/types.ts
frontend/src/App.tsx
验收标准：

viewer 不能改库存
operator 能做业务单据，不能改系统设置
admin 全权限
P0-2 Inbound 单据化
目标：Inbound 和 Outbound 一样，变成 header + lines + movements。

建议新增表：

inbound_documents
inbound_document_lines
建议 header 字段：

id
customer_id
location_id
delivery_date
container_no
document_note
status
created_by
created_at
建议 line 字段：

id
document_id
item_id
sku_snapshot
description_snapshot
expected_qty
received_qty
unit_label
pallets
pallets_detail_ctns
storage_section
line_note
规则：

创建单据时先保存 document 和 lines
post 时再生成 stock_movements
一个柜多个 SKU 只是一张 inbound 单
前端要做：

Inbound 页面切成：
Inbound Documents
Line Items / Activity
批量入库直接对应 inbound document
单条入库改成“新建单据的一行”而不是直接写 movement
当前代码改动点：

database/schema.sql
backend/internal/service 里新增 inbound document store
backend/internal/api/server.go
frontend/src/components/ActivityManagementPage.tsx
frontend/src/lib/api.ts
frontend/src/lib/types.ts
验收标准：

一个 container 可以挂多个 SKU
入库单有 header 和 lines
activity 仍然能看到对应 movement
P0-3 库存调整单
目标：不通过“改库存数值”来修库存，而是通过正式 adjustment 流程。

建议新增表：

inventory_adjustments
inventory_adjustment_lines
建议 header 字段：

adjustment_no
reason_code
notes
status
created_by
建议 line 字段：

item_id
before_qty
adjust_qty
after_qty
storage_section
line_reason
建议原因：

damage
count_gain
count_loss
customer_return
system_correction
规则：

调整必须填原因
确认后生成 ADJUST movement
所有调整都可追踪
前端要做：

新增 Adjustments 页面
支持新建 adjustment document
支持查看历史
验收标准：

所有库存修正都有原因和操作人
All Activity 能看到 adjustment
P0-4 Transfer 转移
目标：支持仓库内和跨仓的库存移动。

建议新增表：

inventory_transfers
inventory_transfer_lines
建议字段：

from_location_id
from_section
to_location_id
to_section
item_id
quantity
status
notes
规则：

transfer 不应改变总库存
只改变 location/section 分布
过账后生成两条 movement 更清楚：
一条负数从原位置出去
一条正数进入新位置
movement type 建议扩成：
IN
OUT
ADJUST
TRANSFER_IN
TRANSFER_OUT
REVERSAL
COUNT
前端要做：

新增 Transfers 页面
支持从当前库存挑选行
支持目标仓库/分区选择
验收标准：

可以从 A 区转去 B 区
可以从 NJ 转去 LA
总库存不变
P0-5 Cycle Count 盘点
目标：正式支持盘点，而不是人工对着表改数。

建议新增表：

cycle_counts
cycle_count_lines
建议 header 字段：

count_no
scope_type
location_id
customer_id
status
created_by
建议 line 字段：

item_id
system_qty
counted_qty
variance_qty
storage_section
line_note
流程：

创建盘点单
选范围
锁定盘点快照
填实盘数
计算差异
确认后生成 adjustment
前端要做：

新增 Cycle Count 页面
支持差异高亮
支持一键生成调整
验收标准：

盘点后能得到 variance
确认后库存自动修正
差异保留历史
P0-6 Outbound 取消 / 冲销
目标：不再通过真删除破坏历史。

建议新增字段或状态：

outbound_documents.status
值至少有：
draft
posted
cancelled
建议规则：

已 posted 的出货单不能直接删
只能 cancel
cancel 时生成 REVERSAL movement 把库存冲回
原单保留，状态改为 cancelled
建议 movement type 增加：

REVERSAL
前端要做：

Outbound 页面把 Delete 改为：
Cancel Shipment
详情页显示状态
PDF 只对 posted 单生效，或对 cancelled 加水印
当前代码改动点：

database/schema.sql
backend/internal/service/outbound_document_store.go
backend/internal/api/server.go
frontend/src/components/ActivityManagementPage.tsx
验收标准：

取消出货单后库存恢复
原单仍保留
All Activity 能看到 reversal
P0 还应该顺手补的基础能力
这几个不一定单独算模块，但正式系统里 P0 很常一起做：

audit_logs
记录谁创建、编辑、取消、调整了什么
created_by / updated_by
document 和 adjustment/count/transfer 都应该带
status 字段统一
inbound/outbound/adjustment/transfer/count 都建议有状态
reason_code
不要全靠自由文本备注
推荐开发顺序
按依赖关系，我建议你这样做：

角色权限
Outbound cancel/reversal
Inbound document 化
Adjustment
Transfer
Cycle Count
Audit log 收口
这样最稳，因为：

权限先做，后面的危险操作都能接进去
Outbound 先收口，避免继续积累真删除逻辑
Inbound/Adjustment/Transfer/Count 再逐步成体系
大概工作量判断

角色权限：中
Outbound cancel/reversal：中
Inbound document 化：大
Adjustment：中
Transfer：中
Cycle Count：中到大
Audit log：中
如果你要我直接开始做
我建议第一阶段先落这 3 个：

角色权限
Outbound cancel/reversal
Inbound document 化